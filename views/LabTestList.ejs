<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>Concept - Bootstrap 4 Admin Dashboard Template</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/css/bootstrap.min.css" />

    <link rel="stylesheet" href="/css/style.css" />

    <style>
      body {
        background-image: linear-gradient(
            rgba(29, 212, 212, 0.664),
            rgba(0, 0, 0, 0.7)
          ),
          url("css/img/1.jpg"), url(img/1.jpg);

        background-size: cover;
      }

      body {
        background: rgb(247, 247, 247);
      }

      hr {
        background: rgb(228, 65, 36);
      }

      .contact-form {
        background: rgb(242, 244, 245);
        color: black;
        margin-top: 20px;

        padding: 100px;

        box-shadow: 0px 0px 10px 3px rgba(66, 64, 64, 0.267);
      }
    </style>
  </head>

  <body>
    <% include partials/labnav %>

    <!-- ============================================================== -->
    <!--  main wrapper -->
    <!-- ============================================================== -->

    <div class="dashboard-wrapper">
      <div class="container-fluid  dashboard-content">
        <!-- ============================================================== -->
        <!-- pageheader -->
        <!-- ============================================================== -->
        <div class="row">
          <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
            <div class="page-header">
              <h2 class="pageheader-title">All Appointment</h2>
              <p class="pageheader-text">
                Proin placerat ante duiullam scelerisque a velit ac porta, fusce
                sit amet vestibulum mi. Morbi lobortis pulvinar quam.
              </p>
              <div class="page-breadcrumb">
                <nav aria-label="breadcrumb">
                  <ol class="breadcrumb"></ol>
                </nav>
              </div>
              <div class="page-breadcrumb">
                <nav aria-label="breadcrumb">
                
                </nav>
              </div>
            </div>
          </div>
        </div>
        <!-- ============================================================== -->
        <!-- basic table  -->
        <!-- ============================================================== -->
        <table class="table table-striped table-bordered first">
          <thead>
            <tr>
              <th>
                Patient Name
              </th>
              <th>Doctor</th>
              <th>Test</th>
              <th>Status</th>
              <th>Add Report</th>
              <th>Done</th>
            </tr>
          </thead>
          <% for(let i = 0; i < posts.length; i++) { %>
          <div class="row">
            <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
              <div class="table-responsive">
                <tbody>
                  <tr>
                    <td><%= posts[i].patientname %></td>
                    <td ><%= posts[i].doctorname%></td>
                    <td><%= posts[i].test %></td>
                    <td><%= posts[i].status %></td>
                    <td>
                        <form
                        id="AddTestReport"
                        >
                        <input hidden id="labid"value="<%= posts[i].labid %>" />
                        <input hidden id="ptname"value="<%= posts[i].patientname %>" />
                        <input hidden id="drname"value="<%= posts[i].doctorname %>" />
                        <input hidden id="ptid"value="<%= posts[i].patientid %>"/>
                        <input hidden id="drid"value="<%= posts[i].doctorid %>"/>
                          <button
                            type="submit"
                            class="btn-danger btn-rounded btn-sm my-0 btn btn-outline-primary"
                            
                          >
                            Add Report
                          </button>
                        </form>
                      </td>
                      <td>
                          <form
                            method="POST"
                            onclick="return confirm('Are you sure?');"
                            action="/lab/test/status/<%= posts[i]._id %>?_method=PUT"
                          >
                            <button
                              type="submit"
                              onclick="return confirm('Are you sure?');"
                              class="btn-danger btn-rounded btn-sm my-0 btn btn-outline-primary"
                              onclick="spjs.contentApproval.toggleApprovelStatus(0);return false;"
                            >
                              Done
                            </button>
                          </form>
                        </td>
                  </tr>
                </tbody>
              </div>
            </div>
          </div>
          <% } %>
        </table>
      </div>
    </div>
    <!-- ============================================================== -->
    <!-- Script -->
    <script rel="javascript" type="text/javascript" src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
    <script rel="javascript" type="text/javascript" src="/main.js"></script>
    <!-- ============================================================== -->
    <!-- ============================================================== -->
  </body>
</html>
